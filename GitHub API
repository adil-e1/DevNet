import requests
import json
import os


token = os.getenv("GITHUB TOKEN")
headers = {"Authorization": f"Bearer {token}"}

repo = requests.get("https://api.github.com/user/repos", headers=headers)

print(json.dumps(repo.json(), indent=2))